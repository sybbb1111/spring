#db config
spring:
  servlet:
    multipart:
      max-file-size: 3MB
      max-request-size: 12MB
  mvc:
    static-path-pattern: /static/**
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/board #???????
    username: root
    password: green502


#mybatis config
mybatis:
  type-aliases-package: com.green.board7 #?? ?? ?? ????
  mapper-locations: mapper/**/*.xml


#swagger
springdoc:
  swagger-ui:
    path: /swagger.html

# 위의 스웨거 내용 붙이면 localhost:8080/swagger.html 하면 바로 뜬다


file:
  dir: D:/download/
